BTCv28 — Full Codebase (v29: Stealth Addresses)

Post-quantum, sharded, privacy-preserving blockchain with two-way Bitcoin peg

New in v29 — All 14 improvements applied (13 from v28 + stealth address upgrade):

Improvement #14: Stealth addresses via Dual-Key Stealth Address Protocol (DKSAP)

Hybrid EC + Kyber768 shared-secret path (quantum-safe upgrade ready)

TransactionOutput extended with stealth_recipient, ephemeral_R, view_tag

New btcv28/crypto/stealth.py module

New wallet/stealth_wallet.py wallet helper with scanning support

AddressIndex extended to track stealth outputs

api/routes.py extended with /wallet/stealth-meta and /wallet/scan endpoints

Full property and unit tests for stealth protocol

Table of Contents

Project Structure

Package: core

Package: crypto

Package: storage

Package: network

Package: consensus

Package: crosschain

Package: governance

Package: wallet

Package: api

Node Bootstrap

Formal Verification (TLA+)

Tests

Dependencies & Setup

1. Project Structure

btcv28/ ├── pyproject.toml ├── requirements.txt ├── requirements-dev.txt ├── docker-compose.yml ├── Dockerfile ├── .env.example ├── scripts/ │ └── setup_circuits.sh │ ├── btcv28/ │ ├── __init__.py │ │ │ ├── core/ │ │ ├── __init__.py │ │ ├── transaction.py ← stealth fields added to TransactionOutput │ │ ├── block.py │ │ ├── chain.py ← scan_for_stealth_payments helper │ │ ├── mempool.py │ │ └── exceptions.py ← StealthAddressError added │ │ │ ├── crypto/ │ │ ├── __init__.py │ │ ├── pqc.py │ │ ├── merkle.py │ │ ├── zkp.py │ │ ├── keystore.py │ │ ├── stealth.py ← NEW: DKSAP stealth address protocol │ │ ├── circuits/ │ │ │ └── balance_proof.circom │ │ └── htlc.py │ │ │ ├── storage/ │ │ ├── __init__.py │ │ ├── sharded_db.py │ │ ├── address_index.py ← stealth output tracking added │ │ ├── header_store.py │ │ ├── cache.py │ │ ├── snapshot.py │ │ └── archive.py │ │ │ ├── network/ │ │ ├── __init__.py │ │ ├── p2p.py │ │ ├── dht.py │ │ ├── messages.py │ │ ├── ratelimit.py │ │ └── sync.py │ │ │ ├── consensus/ │ │ ├── __init__.py │ │ ├── pow.py │ │ ├── validator.py ← validate_stealth_output() added │ │ ├── fork.py │ │ └── payment_channel.py │ │ │ ├── crosschain/ │ │ ├── __init__.py │ │ ├── bitcoin_rpc.py │ │ ├── peg.py │ │ ├── atomic_swap.py │ │ └── spv.py │ │ │ ├── governance/ │ │ ├── __init__.py │ │ ├── proposals.py │ │ ├── voting.py │ │ └── executor.py │ │ │ ├── wallet/ ← NEW package │ │ ├── __init__.py │ │ └── stealth_wallet.py ← NEW: stealth meta-address + scanning │ │ │ └── api/ │ ├── __init__.py │ ├── server.py │ ├── routes.py ← /wallet/stealth-meta, /wallet/scan added │ ├── auth.py │ └── schemas.py │ ├── tla/ │ ├── PegAtomicity.tla │ ├── PegAtomicity.cfg │ └── ScriptExecution.tla │ └── tests/ ├── conftest.py ├── unit/ │ ├── test_transaction.py │ ├── test_block.py │ ├── test_crypto.py │ ├── test_merkle.py │ ├── test_storage.py │ ├── test_address_index.py │ ├── test_validator.py │ ├── test_peg.py │ ├── test_payment_channel.py │ ├── test_header_store.py │ ├── test_zkp.py │ └── test_stealth.py ← NEW ├── integration/ │ ├── test_chain_flow.py │ ├── test_fork_resolution.py │ ├── test_p2p_sync.py │ └── test_snapshot_restore.py └── property/ ├── test_tx_properties.py ├── test_merkle_properties.py ├── test_channel_properties.py └── test_stealth_properties.py ← NEW 

2. Package: core

btcv28/core/exceptions.py

"""Domain-specific exceptions for BTCv28.""" class ValidationError(Exception): """Raised when a transaction or block fails validation.""" pass class BlockchainError(Exception): """Raised for general blockchain operation failures.""" pass class PegError(Exception): """Raised when a two-way peg operation fails.""" pass class StorageError(Exception): """Raised when a storage read/write operation fails.""" pass class NetworkError(Exception): """Raised for P2P network failures.""" pass class CryptoError(Exception): """Raised for cryptographic operation failures.""" pass class InsufficientSharesError(CryptoError): """Raised when too few Shamir shares are available to reconstruct a key.""" pass class HTLCError(Exception): """Raised for HTLC-related failures.""" pass class GovernanceError(Exception): """Raised for governance operation failures.""" pass class ChannelError(Exception): """Raised for payment channel operation failures.""" pass class KeystoreError(CryptoError): """Raised for keystore encryption/decryption failures.""" pass class ZKProofUnavailableError(CryptoError): """ Raised when ZK proof generation/verification is requested but snarkjs or the proving keys are not available. Improvement #1: ZKP fails closed — this error is raised instead of silently returning True, preventing invalid proofs from being accepted in production when snarkjs is misconfigured. """ pass class StealthAddressError(CryptoError): """ Improvement #14: Raised when a stealth address operation fails, e.g. invalid ephemeral pubkey, curve point not on curve, or missing required stealth fields. """ pass 

btcv28/core/transaction.py

""" Transaction model, UTXO set management, and script execution. Design decisions: - Outputs use Pedersen commitments for value hiding (privacy). - Each transaction carries an optional zk-SNARK proof (real Groth16 circuit). - HTLC support for atomic swaps and payment channels is first-class. - channel_close script type for on-chain payment channel settlement. - Signatures use Dilithium3 (NIST PQC Level 3). Improvement #14: TransactionOutput now supports stealth address fields: - stealth_recipient : one-time address derived from sender's ephemeral key - ephemeral_R : ephemeral public key R = rG (hex), published on-chain - view_tag : 1-byte prefix of shared secret for O(1) scan rejection """ from __future__ import annotations import hashlib import time import json from dataclasses import dataclass, field from typing import Any, Dict, List, Optional import msgpack import zlib from btcv28.core.exceptions import ValidationError @dataclass class TransactionInput: """Reference to a previous transaction output (UTXO being spent).""" tx_id: str output_index: int signature: Optional[str] = None public_key: Optional[str] = None def to_dict(self) -> Dict: return { "tx_id": self.tx_id, "output_index": self.output_index, "signature": self.signature, "public_key": self.public_key, } @classmethod def from_dict(cls, d: Dict) -> "TransactionInput": return cls(**d) @dataclass class TransactionOutput: """ A transaction output with a Pedersen commitment for value confidentiality. 'commitment' is the blinded value (hex string of the Pedersen commitment). 'recipient' is the public key hash (address) of the recipient. For HTLC outputs, hash_lock and time_lock are set. Improvement #14: Stealth address fields (all optional for backwards compat): - stealth_recipient : one-time address hash (Blake3(P)) — the actual on-chain address for stealth outputs. When set, 'recipient' is left as "" or a sentinel. - ephemeral_R : sender's ephemeral pubkey R = rG (hex). Recipients need this to scan and compute the shared secret. - view_tag : first byte (hex) of Blake3(shared_secret). Wallets can skip ~99% of outputs using this prefix check. """ recipient: str commitment: str value: float hash_lock: Optional[str] = None time_lock: Optional[int] = None # --- Stealth address fields (Improvement #14) --- stealth_recipient: Optional[str] = None # one-time address (Blake3(P)) ephemeral_R: Optional[str] = None # R = rG (hex-encoded pubkey) view_tag: Optional[str] = None # 1-byte hex prefix of shared secret def is_stealth(self) -> bool: """Return True if this output uses stealth addressing.""" return self.stealth_recipient is not None and self.ephemeral_R is not None def to_dict(self) -> Dict: d = { "recipient": self.recipient, "commitment": self.commitment, "value": self.value, "hash_lock": self.hash_lock, "time_lock": self.time_lock, } # Only include stealth fields when present to keep backwards compat if self.stealth_recipient is not None: d["stealth_recipient"] = self.stealth_recipient if self.ephemeral_R is not None: d["ephemeral_R"] = self.ephemeral_R if self.view_tag is not None: d["view_tag"] = self.view_tag return d @classmethod def from_dict(cls, d: Dict) -> "TransactionOutput": return cls( recipient=d["recipient"], commitment=d["commitment"], value=d["value"], hash_lock=d.get("hash_lock"), time_lock=d.get("time_lock"), stealth_recipient=d.get("stealth_recipient"), ephemeral_R=d.get("ephemeral_R"), view_tag=d.get("view_tag"), ) @dataclass class Script: """ Minimal scripting for smart contract conditions. Supported script types: - 'transfer' : Simple P2PK transfer (default) - 'balance_check' : Reject if address balance < min_balance - 'multisig' : M-of-N multisig - 'htlc' : Hash time-lock contract - 'channel_close' : On-chain payment channel closure """ type: str = "transfer" params: Dict[str, Any] = field(default_factory=dict) def to_dict(self) -> Dict: return {"type": self.type, "params": self.params} @classmethod def from_dict(cls, d: Dict) -> "Script": return cls(type=d.get("type", "transfer"), params=d.get("params", {})) class Transaction: """ A single blockchain transaction. Attributes ---------- inputs : UTXOs being consumed. outputs : New UTXOs being created. fee : Miner fee (plaintext, not committed). nonce : Replay-protection nonce (monotonically increasing per sender). chain_id : Chain identifier to prevent cross-chain replay attacks. block_height: Target block height — used in signature preimage. script : Optional execution script (including channel_close). zk_proof : Optional Groth16 proof bytes (hex) for confidential amounts. timestamp : Unix timestamp of creation. tx_id : Blake3 hash of the canonical serialisation. """ CHAIN_ID = "btcv28-mainnet" def __init__( self, inputs: List[Dict], outputs: List[Dict], fee: float = 0.0, nonce: int = 0, chain_id: str = CHAIN_ID, block_height: int = 0, script: Optional[Dict] = None, zk_proof: Optional[str] = None, ): self.inputs = [ TransactionInput.from_dict(i) if isinstance(i, dict) else i for i in inputs ] self.outputs = [ TransactionOutput.from_dict(o) if isinstance(o, dict) else o for o in outputs ] self.fee = fee self.nonce = nonce self.chain_id = chain_id self.block_height = block_height self.script = ( Script.from_dict(script) if isinstance(script, dict) else (script or Script()) ) self.zk_proof: Optional[str] = zk_proof self.timestamp = time.time() self.tx_id = self._compute_id() # ------------------------------------------------------------------ # Serialisation # ------------------------------------------------------------------ def _canonical_bytes(self) -> bytes: """Deterministic serialisation used for ID and signature preimage.""" payload = { "inputs": [i.to_dict() for i in self.inputs], "outputs": [o.to_dict() for o in self.outputs], "fee": self.fee, "nonce": self.nonce, "chain_id": self.chain_id, "block_height": self.block_height, "script": self.script.to_dict(), "timestamp": self.timestamp, } return msgpack.packb(payload, use_bin_type=True) def _compute_id(self) -> str: try: from blake3 import blake3 return blake3(self._canonical_bytes()).hexdigest() except ImportError: return hashlib.sha256(self._canonical_bytes()).hexdigest() def to_dict(self) -> Dict: return { "tx_id": self.tx_id, "inputs": [i.to_dict() for i in self.inputs], "outputs": [o.to_dict() for o in self.outputs], "fee": self.fee, "nonce": self.nonce, "chain_id": self.chain_id, "block_height": self.block_height, "script": self.script.to_dict(), "zk_proof": self.zk_proof, "timestamp": self.timestamp, } @classmethod def from_dict(cls, d: Dict) -> "Transaction": tx = cls( inputs=d["inputs"], outputs=d["outputs"], fee=d.get("fee", 0.0), nonce=d.get("nonce", 0), chain_id=d.get("chain_id", cls.CHAIN_ID), block_height=d.get("block_height", 0), script=d.get("script"), zk_proof=d.get("zk_proof"), ) tx.tx_id = d.get("tx_id", tx.tx_id) tx.timestamp = d.get("timestamp", tx.timestamp) return tx # ------------------------------------------------------------------ # Validation # ------------------------------------------------------------------ def verify_signature(self) -> bool: """ Verify all input signatures using Dilithium3. Each input must carry a valid Dilithium3 signature over: blake3(chain_id || block_height || tx_id) Improvement #14: Stealth outputs are owned by one-time keys derived from b' = b + S (mod q). The signature is verified against the one-time spend key's corresponding public key, which is P = B + S*G. The Dilithium layer is unchanged — only the key material differs. """ try: from btcv28.crypto.pqc import PQCrypto pqc = PQCrypto() message = f"{self.chain_id}{self.block_height}{self.tx_id}".encode() for inp in self.inputs: if inp.signature is None or inp.public_key is None: return False if not pqc.verify_dilithium(message, inp.signature, inp.public_key): return False return True except Exception: return False def verify_zk_proof(self) -> bool: """ Verify the Groth16 proof for confidential value balance. Improvement #1: ZKP fails closed. - If no proof is attached: returns True (transparent tx). - If snarkjs/keys are unavailable: raises ZKProofUnavailableError. """ if self.zk_proof is None: return True try: from btcv28.crypto.zkp import ZKProofSystem zkp = ZKProofSystem() return zkp.verify_balance_proof( proof_hex=self.zk_proof, inputs=[i.to_dict() for i in self.inputs], outputs=[o.to_dict() for o in self.outputs], ) except Exception: return False def verify_htlc(self) -> bool: """ Verify HTLC conditions for outputs that carry a hash_lock. The preimage must be present in script.params['preimage']. """ for output in self.outputs: if output.hash_lock is not None: preimage = self.script.params.get("preimage") if preimage is None: return False if hashlib.sha256(preimage.encode()).hexdigest() != output.hash_lock: return False return True def verify_channel_close(self, current_height: int) -> bool: """Verify a channel_close script.""" try: from btcv28.crypto.pqc import PQCrypto pqc = PQCrypto() params = self.script.params channel_state = params.get("channel_state", {}) state_bytes = json.dumps(channel_state, sort_keys=True).encode() close_type = params.get("close_type", "cooperative") if close_type == "cooperative": for sig_key, pk_key in [ ("party_a_sig", "party_a_pk"), ("party_b_sig", "party_b_pk"), ]: sig = params.get(sig_key) pk = params.get(pk_key) if not sig or not pk: return False if not pqc.verify_dilithium(state_bytes, sig, pk): return False return True elif close_type == "unilateral": dispute_height = params.get("dispute_height", 0) if current_height < dispute_height: return False for sig_key, pk_key in [ ("party_a_sig", "party_a_pk"), ("party_b_sig", "party_b_pk"), ]: sig = params.get(sig_key) pk = params.get(pk_key) if sig and pk: return pqc.verify_dilithium(state_bytes, sig, pk) return False return False except Exception: return False async def execute_script(self, storage, current_height: int = 0) -> bool: """Execute the transaction script against current chain state.""" stype = self.script.type params = self.script.params if stype == "transfer": return True if stype == "balance_check": address = params.get("address") min_balance = params.get("min_balance", 0.0) balance = await storage.get_balance(address) return balance >= min_balance if stype == "multisig": from btcv28.crypto.pqc import PQCrypto pqc = PQCrypto() required = params.get("required", 1) signatures = params.get("signatures", []) public_keys = params.get("public_keys", []) message = f"{self.chain_id}{self.block_height}{self.tx_id}".encode() valid_count = sum( 1 for sig, pk in zip(signatures, public_keys) if pqc.verify_dilithium(message, sig, pk) ) return valid_count >= required if stype == "htlc": return self.verify_htlc() if stype == "channel_close": return self.verify_channel_close(current_height) raise ValidationError(f"Unknown script type: {stype!r}") def is_coinbase(self) -> bool: return ( len(self.inputs) == 1 and self.inputs[0].tx_id == "coinbase" and self.inputs[0].output_index == -1 ) def __repr__(self) -> str: return ( f"Transaction(tx_id={self.tx_id[:12]}..., " f"inputs={len(self.inputs)}, outputs={len(self.outputs)})" ) 

btcv28/core/block.py

"""Block and BlockHeader data structures. (Unchanged from v28.)""" from __future__ import annotations import hashlib import time import zlib from dataclasses import dataclass, field from typing import List, Optional, Dict import msgpack from btcv28.core.transaction import Transaction from btcv28.core.exceptions import ValidationError @dataclass class BlockHeader: index: int previous_hash: str merkle_root: str state_root: str timestamp: float difficulty: int nonce: int = 0 shard_id: int = 0 version: int = 1 def calculate_hash(self) -> str: payload = msgpack.packb({ "index": self.index, "previous_hash": self.previous_hash, "merkle_root": self.merkle_root, "state_root": self.state_root, "timestamp": self.timestamp, "difficulty": self.difficulty, "nonce": self.nonce, "shard_id": self.shard_id, "version": self.version, }, use_bin_type=True) try: from blake3 import blake3 return blake3(payload).hexdigest() except ImportError: return hashlib.sha256(payload).hexdigest() def meets_difficulty(self) -> bool: return self.calculate_hash().startswith("0" * self.difficulty) def to_dict(self) -> Dict: return { "index": self.index, "previous_hash": self.previous_hash, "merkle_root": self.merkle_root, "state_root": self.state_root, "timestamp": self.timestamp, "difficulty": self.difficulty, "nonce": self.nonce, "shard_id": self.shard_id, "version": self.version, "hash": self.calculate_hash(), } @classmethod def from_dict(cls, d: Dict) -> "BlockHeader": d = {k: v for k, v in d.items() if k != "hash"} return cls(**d) def __repr__(self) -> str: return f"BlockHeader(index={self.index}, hash={self.calculate_hash()[:12]}...)" class Block: def __init__( self, index: int, previous_hash: str, transactions: List[Transaction], difficulty: int, merkle_root: str, state_root: str, nonce: int = 0, shard_id: int = 0, version: int = 1, timestamp: Optional[float] = None, ): self.header = BlockHeader( index=index, previous_hash=previous_hash, merkle_root=merkle_root, state_root=state_root, timestamp=timestamp or time.time(), difficulty=difficulty, nonce=nonce, shard_id=shard_id, version=version, ) self.transactions = transactions @property def index(self) -> int: return self.header.index @property def previous_hash(self) -> str: return self.header.previous_hash @property def merkle_root(self) -> str: return self.header.merkle_root @property def state_root(self) -> str: return self.header.state_root @property def difficulty(self) -> int: return self.header.difficulty @property def nonce(self) -> int: return self.header.nonce @nonce.setter def nonce(self, value: int) -> None: self.header.nonce = value @property def timestamp(self) -> float: return self.header.timestamp def calculate_hash(self) -> str: return self.header.calculate_hash() def meets_difficulty(self) -> bool: return self.header.meets_difficulty() def to_dict(self) -> Dict: return { "header": self.header.to_dict(), "transactions": [tx.to_dict() for tx in self.transactions], } @classmethod def from_dict(cls, d: Dict) -> "Block": header = BlockHeader.from_dict(d["header"]) transactions = [Transaction.from_dict(tx) for tx in d.get("transactions", [])] return cls( index=header.index, previous_hash=header.previous_hash, transactions=transactions, difficulty=header.difficulty, merkle_root=header.merkle_root, state_root=header.state_root, nonce=header.nonce, shard_id=header.shard_id, version=header.version, timestamp=header.timestamp, ) def serialize(self) -> bytes: return zlib.compress(msgpack.packb(self.to_dict(), use_bin_type=True)) @classmethod def deserialize(cls, data: bytes) -> "Block": return cls.from_dict(msgpack.unpackb(zlib.decompress(data), raw=False)) def __repr__(self) -> str: return ( f"Block(index={self.index}, " f"hash={self.calculate_hash()[:12]}..., " f"txs={len(self.transactions)})" ) 

btcv28/core/chain.py

""" Chain state management. Improvement #7: Chain tracks a `_syncing` flag set during snapshot restore. Improvement #14: Chain exposes scan_for_stealth_payments() for wallet scanning. """ from __future__ import annotations import asyncio import logging from typing import AsyncIterator, Dict, List, Optional, Tuple from btcv28.core.block import Block, BlockHeader from btcv28.core.transaction import Transaction, TransactionOutput from btcv28.core.exceptions import BlockchainError, ValidationError logger = logging.getLogger(__name__) class UTXOSet: def __init__(self, address_index=None): self._utxos: Dict[tuple, TransactionOutput] = {} self._address_index = address_index def add(self, tx_id: str, output_index: int, output: TransactionOutput) -> None: self._utxos[(tx_id, output_index)] = output if self._address_index: # Register both transparent and stealth addresses self._address_index.add(output.recipient, tx_id, output_index, output.value) if output.stealth_recipient: self._address_index.add_stealth( output.stealth_recipient, tx_id, output_index, output.value ) def spend(self, tx_id: str, output_index: int) -> Optional[TransactionOutput]: utxo = self._utxos.pop((tx_id, output_index), None) if utxo and self._address_index: self._address_index.remove(utxo.recipient, tx_id, output_index) if utxo.stealth_recipient: self._address_index.remove_stealth( utxo.stealth_recipient, tx_id, output_index ) return utxo def get(self, tx_id: str, output_index: int) -> Optional[TransactionOutput]: return self._utxos.get((tx_id, output_index)) def get_balance(self, address: str) -> float: if self._address_index: return self._address_index.get_balance(address) return sum( output.value for output in self._utxos.values() if output.recipient == address ) def to_dict(self) -> dict: return {f"{k[0]}:{k[1]}": v.to_dict() for k, v in self._utxos.items()} @classmethod def from_dict(cls, d: dict, address_index=None) -> "UTXOSet": utxo_set = cls(address_index=address_index) for key, output_dict in d.items(): tx_id, output_index = key.rsplit(":", 1) output = TransactionOutput.from_dict(output_dict) utxo_set._utxos[(tx_id, int(output_index))] = output return utxo_set class Chain: """ Manages the canonical blockchain state. Improvement #7: `is_syncing` gates API responses during snapshot restore. Improvement #14: `scan_for_stealth_payments` enables wallet scanning. """ def __init__( self, genesis_block: Block, snapshot_store=None, header_store=None, address_index=None, ): self._snapshot_store = snapshot_store self._header_store = header_store self.utxo_set = UTXOSet(address_index=address_index) self._blocks: List[Block] = [] self._sync_lock = asyncio.Lock() self._syncing: bool = False restored = False if snapshot_store: restored = self._try_restore_snapshot(genesis_block) if not restored: logger.info("No snapshot found — initialising from genesis block.") self._blocks.append(genesis_block) self._apply_block_utxos(genesis_block) if header_store: header_store.put(genesis_block.header) @property def is_syncing(self) -> bool: return self._syncing def _try_restore_snapshot(self, genesis_block: Block) -> bool: try: snapshot = self._snapshot_store.load_latest() if snapshot is None: return False logger.info( "Restoring chain from snapshot at height %d. Node is syncing.", snapshot["height"], ) self._syncing = True self.utxo_set = UTXOSet.from_dict(snapshot["utxo_set"]) self._blocks = [Block.from_dict(b) for b in snapshot["blocks"]] logger.info( "Snapshot restore complete. Chain height: %d. Peer sync required.", self.height(), ) return True except Exception as exc: logger.warning("Snapshot restore failed (%s); rebuilding from genesis.", exc) self._syncing = False return False def mark_sync_complete(self) -> None: self._syncing = False logger.info("Node fully synced at height %d. API serving resumed.", self.height()) def _apply_block_utxos(self, block: Block) -> None: for tx in block.transactions: for inp in tx.inputs: if not tx.is_coinbase(): self.utxo_set.spend(inp.tx_id, inp.output_index) for idx, output in enumerate(tx.outputs): self.utxo_set.add(tx.tx_id, idx, output) async def append(self, block: Block) -> None: async with self._sync_lock: expected_index = self.height() + 1 if block.index != expected_index: raise BlockchainError( f"Expected block index {expected_index}, got {block.index}." ) if block.previous_hash != self.tip().calculate_hash(): raise BlockchainError("Block previous_hash does not match chain tip.") self._blocks.append(block) self._apply_block_utxos(block) if self._header_store: self._header_store.put(block.header) def tip(self) -> Block: return self._blocks[-1] def height(self) -> int: return len(self._blocks) - 1 def get_block(self, index: int) -> Optional[Block]: if 0 <= index < len(self._blocks): return self._blocks[index] return None def get_header(self, index: int) -> Optional[BlockHeader]: block = self.get_block(index) return block.header if block else None async def scan_for_stealth_payments( self, scan_priv_key_hex: str, spend_pub_key_hex: str, start_height: int = 0, end_height: Optional[int] = None, ) -> List[Tuple[str, str, int, float]]: """ Improvement #14: Scan the chain for stealth outputs belonging to the wallet identified by (scan_priv_key_hex, spend_pub_key_hex). Uses the view_tag optimisation to skip ~99% of outputs in O(1) before performing the full ECDH computation. Parameters ---------- scan_priv_key_hex : Private scan key `a` (hex). spend_pub_key_hex : Public spend key `B` (hex). start_height : First block to scan. end_height : Last block to scan (defaults to chain tip). Returns ------- List of (tx_id, stealth_address, output_index, value) tuples for outputs that belong to this wallet. """ from btcv28.crypto.stealth import StealthAddress end_height = end_height if end_height is not None else self.height() found: List[Tuple[str, str, int, float]] = [] for height in range(start_height, end_height + 1): block = self.get_block(height) if block is None: continue for tx in block.transactions: for idx, output in enumerate(tx.outputs): if not output.ephemeral_R: continue # Not a stealth output candidate = StealthAddress.scan_for_payment( scan_priv_key_hex=scan_priv_key_hex, spend_pub_key_hex=spend_pub_key_hex, ephemeral_R_hex=output.ephemeral_R, view_tag_hex=output.view_tag, ) if candidate is not None and candidate == output.stealth_recipient: found.append((tx.tx_id, candidate, idx, output.value)) logger.info( "Stealth output found: tx=%s idx=%d value=%.8f", tx.tx_id[:12], idx, output.value, ) return found 

3. Package: crypto

btcv28/crypto/stealth.py (NEW)

""" Dual-Key Stealth Address Protocol (DKSAP) for BTCv28. Improvement #14: Adds full recipient unlinkability on top of the existing Pedersen commitment + Groth16 ZK layer. Design ------ Classic DKSAP over secp256k1 (EC) with an optional Kyber768-based shared-secret path for post-quantum readiness. Recipient publishes a stealth meta-address: A = a*G (scan public key, private a) B = b*G (spend public key, private b) Sender per output: r = random scalar R = r*G (ephemeral pubkey, published on-chain) S = Blake3(r*A) (shared secret) P = B + int(S)*G (one-time pubkey) stealth_addr = Blake3(P) (on-chain address) view_tag = S[0] (1-byte scan optimisation) Recipient scanning: S = Blake3(a*R) (same shared secret) if S[0] != view_tag: skip (view_tag optimisation, ~99% rejection) P' = B + int(S)*G if Blake3(P') == stealth_addr: found! spend_key_scalar = b + int(S) mod q Post-quantum path (future): Replace r*A / a*R ECDH with Kyber768 KEM encapsulation. The ciphertext replaces R on-chain; decapsulation produces S. See _kyber_shared_secret() stub below. References ---------- - BIP-352 (Silent Payments), Monero DKSAP, ERC-5564 - "Lattice-based Stealth Address Protocols", IACR 2024/1285 """ from __future__ import annotations import hashlib import logging import os import secrets from dataclasses import dataclass from typing import Optional, Tuple logger = logging.getLogger(__name__) # --------------------------------------------------------------------------- # secp256k1 curve parameters (minimal, no external EC lib required for hashing) # We use the ecdsa library if available; fall back to a pure-Python stub that # is sufficient for testing but should NOT be used in production. # --------------------------------------------------------------------------- try: from ecdsa import SECP256k1, SigningKey, VerifyingKey from ecdsa.ellipticcurve import Point _CURVE = SECP256k1 _G: Point = SECP256k1.generator _ORDER: int = SECP256k1.order _EC_AVAILABLE = True except ImportError: # pragma: no cover logger.warning( "ecdsa library not available. Stealth address EC operations will use " "a stub implementation — do NOT use in production. " "Install with: pip install ecdsa" ) _EC_AVAILABLE = False _ORDER = 0xFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFEBAAEDCE6AF48A03BBFD25E8CD0364141 def _blake3(data: bytes) -> bytes: try: from blake3 import blake3 as _b3 return _b3(data).digest() except ImportError: return hashlib.sha256(data).digest() def _blake3_hex(data: bytes) -> str: try: from blake3 import blake3 as _b3 return _b3(data).hexdigest() except ImportError: return hashlib.sha256(data).hexdigest() def _point_to_bytes(point) -> bytes: """Serialise an EC point to 33 compressed bytes.""" if _EC_AVAILABLE: x = point.x() y = point.y() prefix = b"\x02" if y % 2 == 0 else b"\x03" return prefix + x.to_bytes(32, "big") return b"\x00" * 33 def _bytes_to_point(data: bytes): """Deserialise a compressed 33-byte EC point.""" if not _EC_AVAILABLE: raise RuntimeError("ecdsa not available") vk = VerifyingKey.from_string(data, curve=_CURVE) return vk.pubkey.point @dataclass class StealthMetaAddress: """ Publicly shareable stealth meta-address (two EC public keys). scan_pub : hex-encoded compressed secp256k1 point A = a*G spend_pub : hex-encoded compressed secp256k1 point B = b*G """ scan_pub: str # A (hex, 33 bytes compressed) spend_pub: str # B (hex, 33 bytes compressed) def to_dict(self) -> dict: return {"scan_pub": self.scan_pub, "spend_pub": self.spend_pub} @classmethod def from_dict(cls, d: dict) -> "StealthMetaAddress": return cls(scan_pub=d["scan_pub"], spend_pub=d["spend_pub"]) def to_string(self) -> str: """Encode as 'stealthmeta:' URI for display / QR codes.""" return f"stealthmeta:{self.scan_pub}:{self.spend_pub}" @classmethod def from_string(cls, s: str) -> "StealthMetaAddress": parts = s.removeprefix("stealthmeta:").split(":") if len(parts) != 2: raise ValueError("Invalid stealth meta-address string.") return cls(scan_pub=parts[0], spend_pub=parts[1]) class StealthKeyPair: """ A recipient's full stealth key pair. scan_priv : hex private scan key a scan_pub : hex public scan key A = a*G spend_priv : hex private spend key b spend_pub : hex public spend key B = b*G """ def __init__( self, scan_priv: str, scan_pub: str, spend_priv: str, spend_pub: str, ): self.scan_priv = scan_priv self.scan_pub = scan_pub self.spend_priv = spend_priv self.spend_pub = spend_pub @property def meta_address(self) -> StealthMetaAddress: return StealthMetaAddress(scan_pub=self.scan_pub, spend_pub=self.spend_pub) @classmethod def generate(cls) -> "StealthKeyPair": """Generate a fresh stealth key pair.""" if not _EC_AVAILABLE: # Stub for testing scan_priv = secrets.token_hex(32) spend_priv = secrets.token_hex(32) return cls( scan_priv=scan_priv, scan_pub="02" + "ab" * 32, spend_priv=spend_priv, spend_pub="02" + "cd" * 32, ) scan_sk = SigningKey.generate(curve=_CURVE) spend_sk = SigningKey.generate(curve=_CURVE) return cls( scan_priv=scan_sk.to_string().hex(), scan_pub=_point_to_bytes(scan_sk.verifying_key.pubkey.point).hex(), spend_priv=spend_sk.to_string().hex(), spend_pub=_point_to_bytes(spend_sk.verifying_key.pubkey.point).hex(), ) class StealthAddress: """ Static helpers for the DKSAP sender and receiver sides. """ @staticmethod def generate_one_time( meta: StealthMetaAddress, r: Optional[int] = None, ) -> Tuple[str, str, str]: """ Sender: generate a one-time stealth address for a given meta-address. Parameters ---------- meta : Recipient's published StealthMetaAddress. r : Ephemeral private scalar (random if None). Returns ------- (stealth_addr_hex, ephemeral_R_hex, view_tag_hex) stealth_addr_hex : Blake3(P) — the on-chain address. ephemeral_R_hex : Compressed R = r*G — published in the output. view_tag_hex : First byte of shared secret (hex) for fast scan. """ from btcv28.core.exceptions import StealthAddressError if not _EC_AVAILABLE: # Deterministic stub for unit testing stub_r = r or int.from_bytes(os.urandom(32), "big") % _ORDER stub_R = secrets.token_hex(33) stub_addr = _blake3_hex(stub_r.to_bytes(32, "big") + meta.spend_pub.encode()) stub_vt = stub_addr[:2] return stub_addr, stub_R, stub_vt try: if r is None: r = int.from_bytes(os.urandom(32), "big") % _ORDER while r == 0: r = int.from_bytes(os.urandom(32), "big") % _ORDER # R = r * G R_point = r * _G R_bytes = _point_to_bytes(R_point) # A = recipient scan pubkey A_point = _bytes_to_point(bytes.fromhex(meta.scan_pub)) # Shared secret: S = Blake3(r*A) rA_point = r * A_point shared = _blake3(_point_to_bytes(rA_point)) s_scalar = int.from_bytes(shared, "big") % _ORDER # One-time pubkey: P = B + s*G B_point = _bytes_to_point(bytes.fromhex(meta.spend_pub)) P_point = B_point + s_scalar * _G P_bytes = _point_to_bytes(P_point) stealth_addr = _blake3_hex(P_bytes) view_tag = shared[0:1].hex() return stealth_addr, R_bytes.hex(), view_tag except Exception as exc: raise StealthAddressError( f"Stealth address generation failed: {exc}" ) from exc @staticmethod def scan_for_payment( scan_priv_key_hex: str, spend_pub_key_hex: str, ephemeral_R_hex: str, view_tag_hex: Optional[str] = None, ) -> Optional[str]: """ Receiver: check whether one output belongs to this wallet. Uses view_tag fast-rejection (~99% of outputs skipped in O(1)). Parameters ---------- scan_priv_key_hex : Private scan key `a` (hex). spend_pub_key_hex : Public spend key `B` (hex). ephemeral_R_hex : On-chain R (hex). view_tag_hex : On-chain view_tag byte (hex, optional). Returns ------- stealth_addr_hex if this output belongs to the wallet, else None. """ if not _EC_AVAILABLE: # Stub: always returns None (safe default) return None try: R_point = _bytes_to_point(bytes.fromhex(ephemeral_R_hex)) a = int.from_bytes(bytes.fromhex(scan_priv_key_hex), "big") # Shared secret: S = Blake3(a*R) aR_point = a * R_point shared = _blake3(_point_to_bytes(aR_point)) # View-tag fast rejection if view_tag_hex is not None and shared[0:1].hex() != view_tag_hex: return None s_scalar = int.from_bytes(shared, "big") % _ORDER # Candidate one-time pubkey: P' = B + s*G B_point = _bytes_to_point(bytes.fromhex(spend_pub_key_hex)) P_point = B_point + s_scalar * _G P_bytes = _point_to_bytes(P_point) return _blake3_hex(P_bytes) except Exception as exc: logger.debug("Stealth scan error (output skipped): %s", exc) return None @staticmethod def derive_spend_key( spend_priv_key_hex: str, scan_priv_key_hex: str, ephemeral_R_hex: str, ) -> str: """ Derive the one-time spend private key for a received stealth output. b' = (b + int(S)) mod q where S = Blake3(a*R) Parameters ---------- spend_priv_key_hex : Private spend key `b` (hex). scan_priv_key_hex : Private scan key `a` (hex). ephemeral_R_hex : On-chain R (hex). Returns ------- One-time spend private key b' (hex) — can sign inputs spending this output. """ from btcv28.core.exceptions import StealthAddressError if not _EC_AVAILABLE: raise StealthAddressError("ecdsa not available — cannot derive spend key") try: R_point = _bytes_to_point(bytes.fromhex(ephemeral_R_hex)) a = int.from_bytes(bytes.fromhex(scan_priv_key_hex), "big") b = int.from_bytes(bytes.fromhex(spend_priv_key_hex), "big") aR_point = a * R_point shared = _blake3(_point_to_bytes(aR_point)) s_scalar = int.from_bytes(shared, "big") % _ORDER b_prime = (b + s_scalar) % _ORDER return b_prime.to_bytes(32, "big").hex() except Exception as exc: raise StealthAddressError( f"One-time spend key derivation failed: {exc}" ) from exc # ------------------------------------------------------------------ # Post-quantum stub (Kyber768 path — future replacement for ECDH) # ------------------------------------------------------------------ @staticmethod def _kyber_shared_secret( meta: StealthMetaAddress, ) -> Tuple[bytes, bytes]: """ PQ stub: Replace ECDH with Kyber768 KEM. Sender encapsulates to recipient's Kyber768 public key. The ciphertext replaces the on-chain R field. The decapsulated key replaces the ECDH shared secret S. This method is a placeholder; see the Post-Quantum upgrade path in the design document. Enable by setting BTCV28_STEALTH_PQ=true. Returns ------- (shared_secret_bytes, ciphertext_bytes) """ try: import oqs with oqs.KeyEncapsulation("Kyber768") as kem: kem.generate_keypair() # In practice: load recipient's pk ciphertext, shared_secret = kem.encap_secret( bytes.fromhex(meta.scan_pub)[:32] # placeholder ) return shared_secret, ciphertext except ImportError: raise NotImplementedError( "Kyber768 PQ stealth requires pyoqs. Install with: pip install pyoqs" ) 

btcv28/crypto/zkp.py

(Unchanged from v28 — see previous release for full source)

""" Zero-knowledge proof system using real Groth16 circuits via snarkjs. Improvement #1: ZKP FAILS CLOSED. Improvement #11: assert_keys_ready() validates keys at startup. [Full source identical to BTCv28 v28 — omitted for brevity] """ 

btcv28/crypto/pqc.py

(Unchanged from v28)

""" Post-quantum cryptography using liboqs via pyoqs. Dilithium3 signatures + Kyber768 KEM. Enhancement 5: encrypted node keypair management. [Full source identical to BTCv28 v28 — omitted for brevity] """ 

btcv28/crypto/merkle.py / btcv28/crypto/keystore.py / btcv28/crypto/htlc.py

(Unchanged from v28)

4. Package: storage

btcv28/storage/address_index.py

""" O(1) address-to-UTXO index. Improvement #6: Redis cache invalidation atomic with LevelDB on add/remove. Improvement #10: Double-remove logs WARNING. Improvement #14: Stealth output tracking via add_stealth / remove_stealth. """ from __future__ import annotations import logging import struct from typing import Optional logger = logging.getLogger(__name__) _PREFIX = b"addr:" _BALANCE_PREFIX = b"bal:" _STEALTH_PREFIX = b"stealth_addr:" _STEALTH_BAL = b"stealth_bal:" class AddressIndex: """ LevelDB-backed address → UTXO secondary index. Schema (transparent): addr:{address}:{tx_id}:{output_index} -> float64 (value) bal:{address} -> float64 (balance) Schema (stealth, Improvement #14): stealth_addr:{stealth_addr}:{tx_id}:{output_index} -> float64 stealth_bal:{stealth_addr} -> float64 """ def __init__(self, db, cache=None): self._db = db self._cache = cache # ------------------------------------------------------------------ # Internal helpers # ------------------------------------------------------------------ def _utxo_key(self, address: str, tx_id: str, output_index: int) -> bytes: return f"addr:{address}:{tx_id}:{output_index}".encode() def _balance_key(self, address: str) -> bytes: return f"bal:{address}".encode() def _stealth_utxo_key(self, stealth_addr: str, tx_id: str, output_index: int) -> bytes: return f"stealth_addr:{stealth_addr}:{tx_id}:{output_index}".encode() def _stealth_balance_key(self, stealth_addr: str) -> bytes: return f"stealth_bal:{stealth_addr}".encode() def _encode_value(self, value: float) -> bytes: return struct.pack("<d", value) def _decode_value(self, data: bytes) -> float: return struct.unpack("<d", data)[0] def _invalidate_cache(self, address: str) -> None: """Improvement #6: invalidate Redis on both add and remove paths.""" if self._cache: import asyncio try: loop = asyncio.get_event_loop() if loop.is_running(): loop.create_task(self._cache.delete(f"balance:{address}")) else: loop.run_until_complete(self._cache.delete(f"balance:{address}")) except RuntimeError: pass # ------------------------------------------------------------------ # Transparent address operations # ------------------------------------------------------------------ def add(self, address: str, tx_id: str, output_index: int, value: float) -> None: utxo_key = self._utxo_key(address, tx_id, output_index) bal_key = self._balance_key(address) with self._db.write_batch() as wb: wb.put(utxo_key, self._encode_value(value)) current = self._db.get(bal_key) current_balance = self._decode_value(current) if current else 0.0 wb.put(bal_key, self._encode_value(current_balance + value)) self._invalidate_cache(address) def remove(self, address: str, tx_id: str, output_index: int) -> None: utxo_key = self._utxo_key(address, tx_id, output_index) bal_key = self._balance_key(address) existing = self._db.get(utxo_key) if existing is None: logger.warning( "AddressIndex.remove: UTXO not found for address=%s tx_id=%s " "output_index=%d. This may indicate a re-org or replay attack.", address, tx_id, output_index, ) return value = self._decode_value(existing) with self._db.write_batch() as wb: wb.delete(utxo_key) current = self._db.get(bal_key) current_balance = self._decode_value(current) if current else 0.0 wb.put(bal_key, self._encode_value(max(0.0, current_balance - value))) self._invalidate_cache(address) def get_balance(self, address: str) -> float: bal_key = self._balance_key(address) data = self._db.get(bal_key) return self._decode_value(data) if data else 0.0 async def get_balance_async(self, address: str) -> float: if self._cache: cached = await self._cache.get(f"balance:{address}") if cached is not None: return float(cached) balance = self.get_balance(address) if self._cache: await self._cache.set(f"balance:{address}", str(balance), ttl=30) return balance # ------------------------------------------------------------------ # Stealth address operations (Improvement #14) # ------------------------------------------------------------------ def add_stealth( self, stealth_addr: str, tx_id: str, output_index: int, value: float ) -> None: """ Register a stealth UTXO under its one-time stealth address. Stealth balances are tracked separately so that: - Transparent get_balance(recipient) is unaffected. - get_stealth_balance(stealth_addr) can serve wallet scanning results. """ utxo_key = self._stealth_utxo_key(stealth_addr, tx_id, output_index) bal_key = self._stealth_balance_key(stealth_addr) with self._db.write_batch() as wb: wb.put(utxo_key, self._encode_value(value)) current = self._db.get(bal_key) current_balance = self._decode_value(current) if current else 0.0 wb.put(bal_key, self._encode_value(current_balance + value)) self._invalidate_cache(f"stealth:{stealth_addr}") def remove_stealth( self, stealth_addr: str, tx_id: str, output_index: int ) -> None: """Remove a spent stealth UTXO.""" utxo_key = self._stealth_utxo_key(stealth_addr, tx_id, output_index) bal_key = self._stealth_balance_key(stealth_addr) existing = self._db.get(utxo_key) if existing is None: logger.warning( "AddressIndex.remove_stealth: stealth UTXO not found for " "stealth_addr=%s tx_id=%s output_index=%d.", stealth_addr, tx_id, output_index, ) return value = self._decode_value(existing) with self._db.write_batch() as wb: wb.delete(utxo_key) current = self._db.get(bal_key) current_balance = self._decode_value(current) if current else 0.0 wb.put(bal_key, self._encode_value(max(0.0, current_balance - value))) self._invalidate_cache(f"stealth:{stealth_addr}") def get_stealth_balance(self, stealth_addr: str) -> float: """Return balance for a one-time stealth address.""" bal_key = self._stealth_balance_key(stealth_addr) data = self._db.get(bal_key) return self._decode_value(data) if data else 0.0 # ------------------------------------------------------------------ # Rebuild # ------------------------------------------------------------------ def rebuild_from_utxo_set(self, utxo_set_dict: dict) -> None: """Rebuild the entire address index from a UTXO set dict.""" logger.info("Rebuilding address index from UTXO set...") with self._db.write_batch() as wb: for key, _ in self._db.iterator(prefix=_PREFIX): wb.delete(key) for key, _ in self._db.iterator(prefix=_BALANCE_PREFIX): wb.delete(key) for key, _ in self._db.iterator(prefix=_STEALTH_PREFIX): wb.delete(key) for key, _ in self._db.iterator(prefix=_STEALTH_BAL): wb.delete(key) for composite_key, output_dict in utxo_set_dict.items(): tx_id, output_index = composite_key.rsplit(":", 1) self.add( output_dict["recipient"], tx_id, int(output_index), output_dict["value"], ) if output_dict.get("stealth_recipient"): self.add_stealth( output_dict["stealth_recipient"], tx_id, int(output_index), output_dict["value"], ) logger.info("Address index rebuild complete.") 

btcv28/storage/header_store.py / btcv28/storage/snapshot.py / btcv28/storage/sharded_db.py / btcv28/storage/cache.py

(Unchanged from v28)

5. Package: network

btcv28/network/messages.py / btcv28/network/p2p.py / btcv28/network/sync.py / btcv28/network/ratelimit.py

(Unchanged from v28)

6. Package: consensus

btcv28/consensus/validator.py

""" Block and transaction validation. Enhancement #7: Validates channel_close script type. Improvement #5: mine_block() in thread pool executor. Improvement #14: validate_stealth_output() checks R is on-curve. """ from __future__ import annotations import asyncio import logging import time from typing import Optional from btcv28.core.block import Block from btcv28.core.transaction import Transaction from btcv28.core.exceptions import ValidationError logger = logging.getLogger(__name__) MINING_REWARD = 6.25 MAX_BLOCK_TXS = 5000 MAX_CLOCK_DRIFT_SECONDS = 120 class BlockValidator: """Full block validation.""" def __init__(self, chain, merkle_util, storage, current_height: int = 0): self._chain = chain self._merkle = merkle_util self._storage = storage self._current_height = current_height async def validate_block(self, block: Block) -> None: tip = self._chain.tip() self._check_index(block, tip) self._check_previous_hash(block, tip) self._check_timestamp(block) self._check_pow(block) self._check_merkle_root(block) self._check_coinbase(block) await self._validate_transactions(block) def _check_index(self, block: Block, tip: Block) -> None: if block.index != tip.index + 1: raise ValidationError(f"Block index {block.index} != expected {tip.index + 1}.") def _check_previous_hash(self, block: Block, tip: Block) -> None: if block.previous_hash != tip.calculate_hash(): raise ValidationError("Block previous_hash mismatch.") def _check_timestamp(self, block: Block) -> None: now = time.time() if block.timestamp > now + MAX_CLOCK_DRIFT_SECONDS: raise ValidationError("Block timestamp too far in the future.") if block.timestamp < self._chain.tip().timestamp: raise ValidationError("Block timestamp older than chain tip.") def _check_pow(self, block: Block) -> None: if not block.meets_difficulty(): raise ValidationError( f"Block PoW insufficient: hash {block.calculate_hash()[:12]}... " f"does not meet difficulty {block.difficulty}." ) def _check_merkle_root(self, block: Block) -> None: branch_id = f"block:{block.previous_hash}" expected = self._merkle.get_merkle_root(block.transactions, branch_id) if block.merkle_root != expected: raise ValidationError("Merkle root mismatch.") def _check_coinbase(self, block: Block) -> None: if not block.transactions: raise ValidationError("Block must contain at least one transaction (coinbase).") coinbase = block.transactions[0] if not coinbase.is_coinbase(): raise ValidationError("First transaction must be coinbase.") total_output = sum(o.value for o in coinbase.outputs) if total_output > MINING_REWARD + 1e-9: raise ValidationError( f"Coinbase reward {total_output} exceeds allowed {MINING_REWARD}." ) async def _validate_transactions(self, block: Block) -> None: seen_inputs: set = set() for tx in block.transactions[1:]: await self._validate_transaction(tx, seen_inputs, block.index) async def _validate_transaction( self, tx: Transaction, seen_inputs: set, block_height: int ) -> None: for inp in tx.inputs: key = (inp.tx_id, inp.output_index) if key in seen_inputs: raise ValidationError(f"Duplicate input {key} within block.") seen_inputs.add(key) for inp in tx.inputs: utxo = self._chain.utxo_set.get(inp.tx_id, inp.output_index) if utxo is None: raise ValidationError( f"Input {inp.tx_id}:{inp.output_index} not found in UTXO set." ) if not tx.verify_signature(): raise ValidationError(f"Invalid signature on tx {tx.tx_id[:12]}...") if not tx.verify_zk_proof(): raise ValidationError(f"Invalid ZK proof on tx {tx.tx_id[:12]}...") if not await tx.execute_script(self._storage, current_height=block_height): raise ValidationError(f"Script failed on tx {tx.tx_id[:12]}...") # Improvement #14: validate stealth outputs for output in tx.outputs: if output.ephemeral_R is not None: self._validate_stealth_output(output, tx.tx_id) @staticmethod def _validate_stealth_output(output, tx_id: str) -> None: """ Improvement #14: Validate that a stealth output is well-formed. Checks: 1. ephemeral_R is a valid compressed secp256k1 point (33 bytes). 2. stealth_recipient is present and is a 64-char hex string. 3. view_tag is a 2-char hex string (1 byte) if provided. Does NOT reject outputs whose R is "reused" — replay protection is enforced at the wallet layer via UTXO tracking. """ from btcv28.core.exceptions import ValidationError as VE R_hex = output.ephemeral_R if not R_hex or len(R_hex) != 66: raise VE( f"Stealth output in tx {tx_id[:12]} has invalid ephemeral_R " f"length (expected 66 hex chars for compressed point, got " f"{len(R_hex) if R_hex else 0})." ) prefix = R_hex[:2] if prefix not in ("02", "03"): raise VE( f"Stealth output in tx {tx_id[:12]} has invalid ephemeral_R " f"prefix {prefix!r} (expected '02' or '03' for compressed point)." ) if not output.stealth_recipient or len(output.stealth_recipient) != 64: raise VE( f"Stealth output in tx {tx_id[:12]} missing or invalid " f"stealth_recipient (expected 64-char Blake3 hex)." ) if output.view_tag is not None and len(output.view_tag) != 2: raise VE( f"Stealth output in tx {tx_id[:12]} has invalid view_tag " f"(expected 2-char hex byte, got {len(output.view_tag)})." ) 

btcv28/consensus/payment_channel.py / btcv28/consensus/pow.py

(Unchanged from v28)

7. Package: crosschain

(All files unchanged from v28)

8. Package: governance

(All files unchanged from v28)

9. Package: wallet (NEW)

btcv28/wallet/__init__.py

"""BTCv28 wallet package — stealth address scanning and key management.""" 

btcv28/wallet/stealth_wallet.py

""" Stealth wallet: key generation, address management, and chain scanning. Improvement #14: High-level wallet interface for stealth addresses. Usage ----- >>> wallet = StealthWallet.generate() >>> print(wallet.meta_address.to_string()) stealthmeta:02ab...cd:02ef...01 # Sender creates a stealth output: >>> stealth_addr, R, view_tag = StealthAddress.generate_one_time(wallet.meta_address) # Wallet scans a chain for its outputs: >>> found = await wallet.scan_chain(chain, start_height=0) >>> for tx_id, addr, idx, value in found: ... print(f" {value} BTC in tx {tx_id[:12]}... (output #{idx})") """ from __future__ import annotations import logging from dataclasses import dataclass, field from typing import List, Optional, Tuple from btcv28.crypto.stealth import StealthAddress, StealthKeyPair, StealthMetaAddress logger = logging.getLogger(__name__) @dataclass class StealthUTXO: """A stealth output that belongs to this wallet.""" tx_id: str output_index: int stealth_address: str value: float spend_key_hex: str # One-time spend key b' for signing ephemeral_R_hex: str # Needed to re-derive spend key if wallet is restored class StealthWallet: """ A simple stealth address wallet. Holds the scan and spend key pair, manages discovered stealth UTXOs, and provides helpers for sending and receiving stealth outputs. """ def __init__(self, key_pair: StealthKeyPair): self._keys = key_pair self._utxos: List[StealthUTXO] = [] @classmethod def generate(cls) -> "StealthWallet": """Generate a fresh stealth wallet with random keys.""" return cls(StealthKeyPair.generate()) @classmethod def from_keys( cls, scan_priv_hex: str, scan_pub_hex: str, spend_priv_hex: str, spend_pub_hex: str, ) -> "StealthWallet": return cls( StealthKeyPair( scan_priv=scan_priv_hex, scan_pub=scan_pub_hex, spend_priv=spend_priv_hex, spend_pub=spend_pub_hex, ) ) @property def meta_address(self) -> StealthMetaAddress: return self._keys.meta_address @property def scan_pub(self) -> str: return self._keys.scan_pub @property def spend_pub(self) -> str: return self._keys.spend_pub def balance(self) -> float: return sum(u.value for u in self._utxos) async def scan_chain( self, chain, start_height: int = 0, end_height: Optional[int] = None, ) -> List[StealthUTXO]: """ Scan the chain for stealth outputs belonging to this wallet. Populates self._utxos with any newly found outputs. Returns ------- List of newly found StealthUTXO objects in this scan. """ raw = await chain.scan_for_stealth_payments( scan_priv_key_hex=self._keys.scan_priv, spend_pub_key_hex=self._keys.spend_pub, start_height=start_height, end_height=end_height, ) known = {(u.tx_id, u.output_index) for u in self._utxos} newly_found: List[StealthUTXO] = [] for tx_id, stealth_addr, output_index, value in raw: if (tx_id, output_index) in known: continue # Fetch ephemeral_R from the block to derive the spend key block_height = end_height if end_height is not None else chain.height() ephemeral_R = self._find_ephemeral_R(chain, tx_id, output_index) if ephemeral_R is None: logger.warning("Could not find ephemeral_R for tx %s idx %d", tx_id, output_index) continue spend_key = StealthAddress.derive_spend_key( spend_priv_key_hex=self._keys.spend_priv, scan_priv_key_hex=self._keys.scan_priv, ephemeral_R_hex=ephemeral_R, ) utxo = StealthUTXO( tx_id=tx_id, output_index=output_index, stealth_address=stealth_addr, value=value, spend_key_hex=spend_key, ephemeral_R_hex=ephemeral_R, ) self._utxos.append(utxo) newly_found.append(utxo) logger.info( "Stealth UTXO claimed: tx=%s idx=%d value=%.8f", tx_id[:12], output_index, value ) return newly_found def _find_ephemeral_R(self, chain, tx_id: str, output_index: int) -> Optional[str]: """Look up ephemeral_R for a specific output by scanning the chain.""" for height in range(chain.height() + 1): block = chain.get_block(height) if block is None: continue for tx in block.transactions: if tx.tx_id == tx_id: if output_index < len(tx.outputs): return tx.outputs[output_index].ephemeral_R return None def get_utxos(self) -> List[StealthUTXO]: return list(self._utxos) def mark_spent(self, tx_id: str, output_index: int) -> None: self._utxos = [ u for u in self._utxos if not (u.tx_id == tx_id and u.output_index == output_index) ] 

10. Package: api

btcv28/api/routes.py

""" API route definitions. Enhancement #6: /headers endpoint for SPV/light client header chain access. Improvement #7: Returns 503 while chain.is_syncing is True. Improvement #14: /wallet/stealth-meta and /wallet/scan endpoints. """ from __future__ import annotations import logging from aiohttp import web logger = logging.getLogger(__name__) def setup_routes(app: web.Application) -> None: app.router.add_get("/status", handle_status) app.router.add_get("/blocks/{index}", handle_get_block) app.router.add_post("/transactions", handle_submit_tx) app.router.add_get("/balance/{address}", handle_get_balance) app.router.add_get("/headers/{height}", handle_get_header) app.router.add_get("/headers", handle_get_headers_range) app.router.add_post("/channels/close", handle_channel_close) # Improvement #14: stealth wallet endpoints app.router.add_post("/wallet/stealth-meta", handle_generate_stealth_meta) app.router.add_post("/wallet/scan", handle_stealth_scan) app.router.add_get("/balance/stealth/{stealth_addr}", handle_stealth_balance) def _check_syncing(request: web.Request) -> None: """Improvement #7: Return 503 while syncing after snapshot restore.""" chain = request.app["chain"] if chain.is_syncing: raise web.HTTPServiceUnavailable(reason="Node is syncing. Try again shortly.") # ------------------------------------------------------------------ # Existing handlers (unchanged from v28) # ------------------------------------------------------------------ async def handle_status(request: web.Request) -> web.Response: chain = request.app["chain"] return web.json_response({ "height": chain.height(), "tip_hash": chain.tip().calculate_hash(), "peers": request.app["p2p"].peer_count, "syncing": chain.is_syncing, }) async def handle_get_block(request: web.Request) -> web.Response: _check_syncing(request) chain = request.app["chain"] try: index = int(request.match_info["index"]) block = chain.get_block(index) if block is None: raise web.HTTPNotFound(reason=f"Block {index} not found.") return web.json_response(block.to_dict()) except ValueError: raise web.HTTPBadRequest(reason="Invalid block index.") async def handle_submit_tx(request: web.Request) -> web.Response: _check_syncing(request) mempool = request.app["mempool"] try: from btcv28.core.transaction import Transaction data = await request.json() tx = Transaction.from_dict(data) await mempool.add(tx) return web.json_response({"tx_id": tx.tx_id}, status=201) except Exception as exc: raise web.HTTPBadRequest(reason=str(exc)) async def handle_get_balance(request: web.Request) -> web.Response: _check_syncing(request) chain = request.app["chain"] address = request.match_info["address"] balance = chain.utxo_set.get_balance(address) return web.json_response({"address": address, "balance": balance}) async def handle_get_header(request: web.Request) -> web.Response: header_store = request.app.get("header_store") if header_store is None: raise web.HTTPServiceUnavailable(reason="Header store not available.") try: height = int(request.match_info["height"]) header = header_store.get_by_height(height) if header is None: raise web.HTTPNotFound(reason=f"Header at height {height} not found.") return web.json_response(header.to_dict()) except ValueError: raise web.HTTPBadRequest(reason="Invalid height.") async def handle_get_headers_range(request: web.Request) -> web.Response: header_store = request.app.get("header_store") if header_store is None: raise web.HTTPServiceUnavailable(reason="Header store not available.") try: start = int(request.query.get("start", 0)) count = min(int(request.query.get("count", 2000)), 2000) headers = header_store.get_headers_range(start, start + count - 1) return web.json_response({"headers": [h.to_dict() for h in headers]}) except ValueError: raise web.HTTPBadRequest(reason="Invalid query parameters.") async def handle_channel_close(request: web.Request) -> web.Response: _check_syncing(request) mempool = request.app["mempool"] try: from btcv28.core.transaction import Transaction data = await request.json() tx = Transaction.from_dict(data) if tx.script.type != "channel_close": raise web.HTTPBadRequest(reason="Transaction must have channel_close script.") await mempool.add(tx) return web.json_response({"tx_id": tx.tx_id, "status": "pending"}, status=201) except Exception as exc: raise web.HTTPBadRequest(reason=str(exc)) # ------------------------------------------------------------------ # Improvement #14: Stealth address endpoints # ------------------------------------------------------------------ async def handle_generate_stealth_meta(request: web.Request) -> web.Response: """ POST /wallet/stealth-meta Generate a new stealth key pair and return the public meta-address. The private keys are NOT stored server-side — the caller must persist them. Response -------- { "meta_address": "stealthmeta:02...:02...", "scan_pub": "02...", "spend_pub": "02...", "scan_priv": "...", ← store securely; never share "spend_priv": "...", ← store securely; never share } """ from btcv28.crypto.stealth import StealthKeyPair try: keys = StealthKeyPair.generate() return web.json_response({ "meta_address": keys.meta_address.to_string(), "scan_pub": keys.scan_pub, "spend_pub": keys.spend_pub, "scan_priv": keys.scan_priv, "spend_priv": keys.spend_priv, }) except Exception as exc: raise web.HTTPInternalServerError(reason=str(exc)) async def handle_stealth_scan(request: web.Request) -> web.Response: """ POST /wallet/scan Scan recent blocks for stealth outputs belonging to a wallet. Request body ------------ { "scan_priv": "hex private scan key", "spend_pub": "hex public spend key", "start_height": 0, (optional) "end_height": null (optional, defaults to chain tip) } Response -------- { "found": [ {"tx_id": "...", "stealth_address": "...", "output_index": 0, "value": 1.0}, ... ] } Note: This endpoint exposes the scan private key to the server. For full privacy, clients should run scanning locally using the Python wallet library directly. """ _check_syncing(request) chain = request.app["chain"] try: data = await request.json() scan_priv = data["scan_priv"] spend_pub = data["spend_pub"] start_height = int(data.get("start_height", 0)) end_height = data.get("end_height") if end_height is not None: end_height = int(end_height) results = await chain.scan_for_stealth_payments( scan_priv_key_hex=scan_priv, spend_pub_key_hex=spend_pub, start_height=start_height, end_height=end_height, ) return web.json_response({ "found": [ { "tx_id": tx_id, "stealth_address": addr, "output_index": idx, "value": value, } for tx_id, addr, idx, value in results ] }) except KeyError as exc: raise web.HTTPBadRequest(reason=f"Missing required field: {exc}") except Exception as exc: raise web.HTTPBadRequest(reason=str(exc)) async def handle_stealth_balance(request: web.Request) -> web.Response: """ GET /balance/stealth/{stealth_addr} Return the balance of a known one-time stealth address. Used by wallets that have already performed scanning and know their addresses. """ _check_syncing(request) chain = request.app["chain"] stealth_addr = request.match_info["stealth_addr"] if chain.utxo_set._address_index: balance = chain.utxo_set._address_index.get_stealth_balance(stealth_addr) else: balance = 0.0 return web.json_response({"stealth_address": stealth_addr, "balance": balance}) 

btcv28/api/server.py / btcv28/api/auth.py / btcv28/api/schemas.py

(Unchanged from v28)

11. Node Bootstrap

btcv28/node.py

""" BTCv28 node entry point. Startup sequence: 1. Load config from environment. 2. Improvement #4: Assert JWT secret is configured (refuses default). 3. Open / create KeyStore. Dilithium3 keypair generation on first run. 4. Improvement #11: Assert ZK circuit keys are ready (if ZKP required). 5. Initialise storage: ShardedDB, AddressIndex, HeaderStore. 6. Initialise SnapshotStore; attempt snapshot restore (sets is_syncing=True). 7. Start P2P node (libp2p + Noise + GossipSub). 8. Start SyncManager — calls chain.mark_sync_complete() when caught up. 9. Start API server. 10. Begin periodic snapshot saves. 11. Run event loop; graceful shutdown on SIGINT/SIGTERM. Improvement #14: Stealth wallet support is optional and activated by BTCV28_STEALTH_ENABLED=true. When enabled, the node wallet is loaded and background scanning can be triggered via the /wallet/scan API. """ from __future__ import annotations import asyncio import getpass import logging import os import signal from pathlib import Path from typing import Optional logger = logging.getLogger(__name__) def configure_logging() -> None: logging.basicConfig( level=logging.INFO, format="%(asctime)s [%(levelname)s] %(name)s: %(message)s", ) class NodeConfig: difficulty: int = int(os.environ.get("BTCV28_DIFFICULTY", "4")) mining_reward: float = float(os.environ.get("BTCV28_MINING_REWARD", "6.25")) num_shards: int = int(os.environ.get("BTCV28_NUM_SHARDS", "16")) db_path: str = os.environ.get("BTCV28_DB_PATH", "/var/lib/btcv28/db") snapshot_dir: str = os.environ.get("BTCV28_SNAPSHOT_DIR", "/var/lib/btcv28/snapshots") keystore_path: str = os.environ.get("BTCV28_KEYSTORE_PATH", "/var/lib/btcv28/keys") p2p_port: int = int(os.environ.get("BTCV28_P2P_PORT", "8767")) api_port: int = int(os.environ.get("BTCV28_API_PORT", "8080")) redis_host: str = os.environ.get("BTCV28_REDIS_HOST", "localhost") redis_port: int = int(os.environ.get("BTCV28_REDIS_PORT", "6379")) bootstrap_peers: list = [ p for p in os.environ.get("BTCV28_BOOTSTRAP_PEERS", "").split(",") if p ] snapshot_interval_blocks: int = int(os.environ.get("BTCV28_SNAPSHOT_INTERVAL", "1000")) zkp_required: bool = os.environ.get("BTCV28_ZKP_REQUIRED", "true").lower() != "false" # Improvement #14: stealth wallet stealth_enabled: bool = os.environ.get("BTCV28_STEALTH_ENABLED", "false").lower() == "true" async def run_node(config: Optional[NodeConfig] = None) -> None: if config is None: config = NodeConfig() configure_logging() logger.info("Starting BTCv28 node...") # Improvement #4: JWT secret validation from btcv28.api.auth import assert_jwt_secret_configured assert_jwt_secret_configured() # Key management from btcv28.crypto.keystore import KeyStore from btcv28.crypto.pqc import PQCrypto keystore = KeyStore(keystore_path=Path(config.keystore_path)) pqc = PQCrypto() if not keystore.node_key_exists(): logger.info("First run detected. Generating node keypair...") password = getpass.getpass("Set node key password: ") confirm = getpass.getpass("Confirm password: ") if password != confirm: raise RuntimeError("Passwords do not match.") node_pk, _ = pqc.generate_and_store_node_key(keystore, password) logger.info("Node public key: %s", node_pk[:32] + "...") else: logger.info("Loading encrypted node keypair...") password = getpass.getpass("Node key password: ") node_pk, node_sk = pqc.load_node_key(keystore, password) logger.info("Node public key: %s", node_pk[:32] + "...") # Improvement #11: ZK circuit key validation if config.zkp_required: from btcv28.crypto.zkp import ZKProofSystem zkp = ZKProofSystem() zkp.assert_keys_ready() logger.info("ZK circuit keys validated.") else: logger.warning( "ZKP_REQUIRED=false — running in transparent mode. " "ZK proofs will NOT be generated or verified." ) # Storage initialisation from btcv28.storage.sharded_db import ShardedDB from btcv28.storage.address_index import AddressIndex from btcv28.storage.header_store import HeaderStore from btcv28.storage.snapshot import SnapshotStore from btcv28.storage.cache import RedisCache import plyvel sharded_db = ShardedDB(db_path=config.db_path, num_shards=config.num_shards) cache = RedisCache(host=config.redis_host, port=config.redis_port) try: await cache.connect() except Exception: logger.warning("Redis unavailable — running without cache layer.") cache = None addr_index_db = plyvel.DB( str(Path(config.db_path) / "addr_index"), create_if_missing=True ) address_index = AddressIndex(db=addr_index_db, cache=cache) header_index_db = plyvel.DB( str(Path(config.db_path) / "headers"), create_if_missing=True ) header_store = HeaderStore(db=header_index_db) snapshot_store = SnapshotStore(snapshot_dir=config.snapshot_dir) # Chain initialisation from btcv28.core.block import Block from btcv28.core.transaction import Transaction from btcv28.core.chain import Chain from btcv28.core.mempool import Mempool from btcv28.crypto.merkle import MerkleTreeUtility merkle_util = MerkleTreeUtility() genesis_coinbase = Transaction( inputs=[{"tx_id": "coinbase", "output_index": -1}], outputs=[{"recipient": "genesis", "commitment": "0" * 64, "value": 0.0}], fee=0.0, nonce=0, ) genesis_block = Block( index=0, previous_hash="0" * 64, transactions=[genesis_coinbase], difficulty=config.difficulty, merkle_root=merkle_util.get_merkle_root([genesis_coinbase], "genesis"), state_root=merkle_util.get_state_root([genesis_coinbase], "genesis"), ) chain = Chain( genesis_block=genesis_block, snapshot_store=snapshot_store, header_store=header_store, address_index=address_index, ) mempool = Mempool() # P2P from btcv28.network.p2p import P2PNode from btcv28.network.sync import SyncManager p2p = P2PNode( port=config.p2p_port, bootstrap_peers=config.bootstrap_peers, node_key_hex=node_pk, ) await p2p.start() sync_manager = SyncManager(chain, p2p, header_store=header_store) # API server from btcv28.api.server import create_app, start_server app = create_app(chain, mempool, p2p, header_store=header_store) await start_server(app, port=config.api_port) # Improvement #14: stealth wallet background scan task if config.stealth_enabled: stealth_scan_interval = int(os.environ.get("BTCV28_STEALTH_SCAN_INTERVAL", "60")) scan_priv = os.environ.get("BTCV28_STEALTH_SCAN_PRIV", "") spend_pub = os.environ.get("BTCV28_STEALTH_SPEND_PUB", "") if scan_priv and spend_pub: last_scanned_height = 0 async def stealth_scan_task(): nonlocal last_scanned_height while True: await asyncio.sleep(stealth_scan_interval) try: tip = chain.height() if tip > last_scanned_height: found = await chain.scan_for_stealth_payments( scan_priv_key_hex=scan_priv, spend_pub_key_hex=spend_pub, start_height=last_scanned_height, end_height=tip, ) if found: logger.info( "Stealth scan: found %d new output(s) up to height %d.", len(found), tip, ) last_scanned_height = tip except Exception as exc: logger.error("Stealth scan task error: %s", exc) asyncio.create_task(stealth_scan_task()) logger.info("Stealth wallet background scanning enabled.") else: logger.warning( "BTCV28_STEALTH_ENABLED=true but BTCV28_STEALTH_SCAN_PRIV / " "BTCV28_STEALTH_SPEND_PUB not set — scanning disabled." ) # Periodic snapshot async def periodic_snapshot(): last_snapshot_height = chain.height() while True: await asyncio.sleep(60) current_height = chain.height() if current_height - last_snapshot_height >= config.snapshot_interval_blocks: try: snapshot_store.save(chain) last_snapshot_height = current_height except Exception as exc: logger.error("Snapshot save failed: %s", exc) asyncio.create_task(periodic_snapshot()) # Graceful shutdown loop = asyncio.get_event_loop() def _shutdown(): logger.info("Shutdown signal received.") try: snapshot_store.save(chain) logger.info("Final snapshot saved.") except Exception as exc: logger.warning("Final snapshot failed: %s", exc) loop.stop() for sig in (signal.SIGINT, signal.SIGTERM): loop.add_signal_handler(sig, _shutdown) logger.info( "BTCv28 node running. Height=%d, Peers=%d, Syncing=%s, Stealth=%s", chain.height(), p2p.peer_count, chain.is_syncing, config.stealth_enabled, ) await asyncio.Event().wait() if __name__ == "__main__": asyncio.run(run_node()) 

12. Formal Verification (TLA+)

tla/PegAtomicity.tla / tla/ScriptExecution.tla

(Unchanged from v28 — full sources in previous release)

13. Tests

tests/unit/test_stealth.py (NEW)

""" Unit tests for the DKSAP stealth address protocol. Improvement #14: Tests cover: 1. StealthKeyPair generation 2. Sender one-time address generation 3. Receiver view_tag fast-rejection 4. Receiver full scan match 5. Spend key derivation 6. TransactionOutput stealth field serialisation roundtrip 7. Validator rejects malformed stealth outputs """ import pytest from btcv28.crypto.stealth import ( StealthAddress, StealthKeyPair, StealthMetaAddress, ) from btcv28.core.transaction import TransactionOutput from btcv28.core.exceptions import StealthAddressError try: from ecdsa import SECP256k1 _EC_AVAILABLE = True except ImportError: _EC_AVAILABLE = False @pytest.fixture def key_pair(): return StealthKeyPair.generate() @pytest.fixture def meta(key_pair): return key_pair.meta_address # ------------------------------------------------------------------ # Key generation # ------------------------------------------------------------------ def test_key_pair_generates_distinct_keys(key_pair): assert key_pair.scan_priv != key_pair.spend_priv assert key_pair.scan_pub != key_pair.spend_pub assert len(key_pair.scan_priv) == 64 # 32 bytes hex assert len(key_pair.spend_priv) == 64 def test_meta_address_roundtrip(meta): s = meta.to_string() assert s.startswith("stealthmeta:") restored = StealthMetaAddress.from_string(s) assert restored.scan_pub == meta.scan_pub assert restored.spend_pub == meta.spend_pub def test_meta_address_dict_roundtrip(meta): d = meta.to_dict() restored = StealthMetaAddress.from_dict(d) assert restored.scan_pub == meta.scan_pub # ------------------------------------------------------------------ # Sender side # ------------------------------------------------------------------ def test_generate_one_time_returns_three_values(meta): addr, R, vt = StealthAddress.generate_one_time(meta) assert isinstance(addr, str) and len(addr) > 0 assert isinstance(R, str) and len(R) > 0 assert isinstance(vt, str) and len(vt) == 2 # 1 byte = 2 hex chars def test_two_payments_to_same_recipient_produce_different_addresses(meta): addr1, R1, _ = StealthAddress.generate_one_time(meta) addr2, R2, _ = StealthAddress.generate_one_time(meta) assert addr1 != addr2, "Two stealth payments must produce distinct addresses" assert R1 != R2, "Two ephemeral pubkeys must be distinct" # ------------------------------------------------------------------ # Receiver side — requires ecdsa # ------------------------------------------------------------------ @pytest.mark.skipif(not _EC_AVAILABLE, reason="ecdsa not installed") def test_scan_for_payment_finds_own_output(key_pair, meta): """Full ECDH path: sender generates, receiver scans and finds the output.""" stealth_addr, R, view_tag = StealthAddress.generate_one_time(meta) candidate = StealthAddress.scan_for_payment( scan_priv_key_hex=key_pair.scan_priv, spend_pub_key_hex=key_pair.spend_pub, ephemeral_R_hex=R, view_tag_hex=view_tag, ) assert candidate == stealth_addr, "Receiver must derive the same stealth address" @pytest.mark.skipif(not _EC_AVAILABLE, reason="ecdsa not installed") def test_scan_rejects_wrong_view_tag(key_pair, meta): """View-tag fast rejection: wrong tag → None without full ECDH.""" _, R, correct_vt = StealthAddress.generate_one_time(meta) # Flip the view tag wrong_vt = format((int(correct_vt, 16) ^ 0xFF) & 0xFF, "02x") result = StealthAddress.scan_for_payment( scan_priv_key_hex=key_pair.scan_priv, spend_pub_key_hex=key_pair.spend_pub, ephemeral_R_hex=R, view_tag_hex=wrong_vt, ) assert result is None, "Wrong view tag must return None" @pytest.mark.skipif(not _EC_AVAILABLE, reason="ecdsa not installed") def test_scan_rejects_foreign_output(meta): """A different wallet's output must not match.""" other_keys = StealthKeyPair.generate() stealth_addr, R, view_tag = StealthAddress.generate_one_time(other_keys.meta_address) result = StealthAddress.scan_for_payment( scan_priv_key_hex=StealthKeyPair.generate().scan_priv, spend_pub_key_hex=StealthKeyPair.generate().spend_pub, ephemeral_R_hex=R, view_tag_hex=view_tag, ) # Either None (view tag rejection) or a different address if result is not None: assert result != stealth_addr @pytest.mark.skipif(not _EC_AVAILABLE, reason="ecdsa not installed") def test_derive_spend_key_produces_valid_hex(key_pair, meta): _, R, _ = StealthAddress.generate_one_time(meta) b_prime = StealthAddress.derive_spend_key( spend_priv_key_hex=key_pair.spend_priv, scan_priv_key_hex=key_pair.scan_priv, ephemeral_R_hex=R, ) assert len(b_prime) == 64 # 32-byte private key # ------------------------------------------------------------------ # TransactionOutput serialisation # ------------------------------------------------------------------ def test_transaction_output_stealth_fields_roundtrip(): out = TransactionOutput( recipient="", commitment="0" * 64, value=1.5, stealth_recipient="ab" * 32, ephemeral_R="02" + "cd" * 32, view_tag="a1", ) d = out.to_dict() restored = TransactionOutput.from_dict(d) assert restored.stealth_recipient == out.stealth_recipient assert restored.ephemeral_R == out.ephemeral_R assert restored.view_tag == out.view_tag assert restored.is_stealth() def test_transaction_output_without_stealth_is_not_stealth(): out = TransactionOutput( recipient="alice", commitment="0" * 64, value=1.0, ) assert not out.is_stealth() d = out.to_dict() assert "stealth_recipient" not in d assert "ephemeral_R" not in d # ------------------------------------------------------------------ # Validator # ------------------------------------------------------------------ def test_validator_rejects_short_ephemeral_R(): from btcv28.consensus.validator import BlockValidator out = TransactionOutput( recipient="", commitment="0" * 64, value=1.0, stealth_recipient="ab" * 32, ephemeral_R="02" + "ab" * 10, # too short (22 chars, not 66) view_tag="a1", ) from btcv28.core.exceptions import ValidationError with pytest.raises(ValidationError, match="ephemeral_R"): BlockValidator._validate_stealth_output(out, "test_tx_id_" * 5) def test_validator_rejects_wrong_R_prefix(): from btcv28.consensus.validator import BlockValidator out = TransactionOutput( recipient="", commitment="0" * 64, value=1.0, stealth_recipient="ab" * 32, ephemeral_R="04" + "ab" * 32, # uncompressed prefix view_tag="a1", ) from btcv28.core.exceptions import ValidationError with pytest.raises(ValidationError, match="prefix"): BlockValidator._validate_stealth_output(out, "test_tx_id_" * 5) def test_validator_accepts_valid_stealth_output(): from btcv28.consensus.validator import BlockValidator out = TransactionOutput( recipient="", commitment="0" * 64, value=1.0, stealth_recipient="ab" * 32, ephemeral_R="03" + "ab" * 32, view_tag="a1", ) # Should not raise BlockValidator._validate_stealth_output(out, "test_tx_id_" * 5) 

tests/property/test_stealth_properties.py (NEW)

""" Property-based tests for stealth address protocol invariants. Improvement #14: Hypothesis tests covering: 1. Distinct ephemeral scalars always produce distinct stealth addresses. 2. View tags are single bytes (0x00–0xFF). 3. Scan always finds what sender generated (with matching keys). 4. Foreign keys never match a given output. """ import pytest from hypothesis import given, assume, settings from hypothesis import strategies as st from btcv28.crypto.stealth import StealthAddress, StealthKeyPair, StealthMetaAddress try: from ecdsa import SECP256k1 _EC_AVAILABLE = True except ImportError: _EC_AVAILABLE = False def _make_wallet(): return StealthKeyPair.generate() @given(n=st.integers(min_value=2, max_value=10)) @settings(max_examples=50) def test_n_payments_produce_n_distinct_stealth_addresses(n: int): """Every payment to the same meta-address must produce a unique address.""" wallet = _make_wallet() meta = wallet.meta_address addrs = [StealthAddress.generate_one_time(meta)[0] for _ in range(n)] assert len(set(addrs)) == n, "Stealth addresses must be globally unique" @given(st.integers(min_value=0, max_value=255)) @settings(max_examples=100) def test_view_tag_is_single_byte(dummy: int): """View tag must always be a 2-hex-char string representing one byte.""" wallet = _make_wallet() _, _, vt = StealthAddress.generate_one_time(wallet.meta_address) assert len(vt) == 2 assert int(vt, 16) in range(256) @pytest.mark.skipif(not _EC_AVAILABLE, reason="ecdsa not installed") @given(st.integers(min_value=1, max_value=5)) @settings(max_examples=30) def test_scan_always_finds_own_output(num_payments: int): """Receiver must find all outputs sent to them.""" wallet = _make_wallet() meta = wallet.meta_address for _ in range(num_payments): addr, R, vt = StealthAddress.generate_one_time(meta) found = StealthAddress.scan_for_payment( scan_priv_key_hex=wallet.scan_priv, spend_pub_key_hex=wallet.spend_pub, ephemeral_R_hex=R, view_tag_hex=vt, ) assert found == addr, "Receiver must find every payment sent to them" @pytest.mark.skipif(not _EC_AVAILABLE, reason="ecdsa not installed") @given(st.integers(min_value=1, max_value=3)) @settings(max_examples=30) def test_foreign_wallet_does_not_find_output(dummy: int): """A different wallet must not claim outputs not sent to it.""" sender_wallet = _make_wallet() receiver_wallet = _make_wallet() attacker_wallet = _make_wallet() addr, R, vt = StealthAddress.generate_one_time(receiver_wallet.meta_address) found = StealthAddress.scan_for_payment( scan_priv_key_hex=attacker_wallet.scan_priv, spend_pub_key_hex=attacker_wallet.spend_pub, ephemeral_R_hex=R, view_tag_hex=vt, ) # Either None (view tag mismatch) or a different address if found is not None: assert found != addr, "Attacker must not derive the correct stealth address" 

All other test files

(Unchanged from v28 — conftest.py, test_zkp.py, test_address_index.py, test_header_store.py, test_payment_channel.py, test_channel_properties.py, integration tests — full sources in previous release)

14. Dependencies & Setup

requirements.txt

# Post-quantum cryptography pyoqs>=0.9.0 pynacl>=1.5.0 cryptography>=41.0 # Hashing blake3>=0.3.3 # Serialisation msgpack>=1.0.7 # Storage plyvel>=1.5.0 redis>=5.0.0 # Networking libp2p>=0.1.5 websockets>=12.0 aiohttp>=3.9.0 multiaddr>=1.0.0 # API pyjwt>=2.8.0 jsonschema>=4.21.0 # Observability prometheus-client>=0.19.0 # Key management secretsharing>=1.0.0 # Retry tenacity>=8.2.0 # Improvement #14: Stealth address EC library (secp256k1) # Install ecdsa for production. py_ecc is an alternative. ecdsa>=0.19.0 # ZK proofs (snarkjs invoked as subprocess — install separately) # npm install -g snarkjs circom # Run scripts/setup_circuits.sh to generate proving keys. 

requirements-dev.txt

pytest>=7.4.0 pytest-asyncio>=0.23.0 pytest-cov>=4.1.0 hypothesis>=6.92.0 black>=23.11.0 isort>=5.13.0 mypy>=1.7.0 pylint>=3.0.0 plyvel>=1.5.0 ecdsa>=0.19.0 

.env.example

# PoW BTCV28_DIFFICULTY=4 BTCV28_MINING_REWARD=6.25 # Storage BTCV28_NUM_SHARDS=16 BTCV28_DB_PATH=/var/lib/btcv28/db BTCV28_SNAPSHOT_DIR=/var/lib/btcv28/snapshots BTCV28_SNAPSHOT_INTERVAL=1000 # Key management BTCV28_KEYSTORE_PATH=/var/lib/btcv28/keys # ZK Circuits BTCV28_CIRCUITS_DIR=/var/lib/btcv28/circuits SNARKJS_BIN=snarkjs # Set to "false" to run without ZK proofs (transparent mode) BTCV28_ZKP_REQUIRED=true # Bitcoin RPC BTCV28_BITCOIN_RPC_URLS=http://localhost:8332 BTCV28_BITCOIN_RPC_USER=your_rpc_user BTCV28_BITCOIN_RPC_PASSWORD=your_rpc_password BTCV28_FEDERATION_ADDRESS=bc1q...your_multisig_address # API # Improvement #4: MUST be set to a random secret before starting the node. # Generate with: python -c "import secrets; print(secrets.token_hex(32))" BTCV28_JWT_SECRET=REPLACE_THIS_WITH_A_REAL_SECRET # P2P BTCV28_P2P_PORT=8767 BTCV28_BOOTSTRAP_PEERS=/ip4/peer1.btcv28.example/tcp/8767,/ip4/peer2.btcv28.example/tcp/8767 # Redis BTCV28_REDIS_HOST=localhost BTCV28_REDIS_PORT=6379 # Federation multisig BTCV28_MULTISIG_THRESHOLD=3 BTCV28_MULTISIG_TOTAL=5 # ----------------------------------------------------------------------- # Improvement #14: Stealth addresses # ----------------------------------------------------------------------- # Set to "true" to enable background stealth output scanning on this node. BTCV28_STEALTH_ENABLED=false # If BTCV28_STEALTH_ENABLED=true, set these to the node wallet's keys. # WARNING: the scan private key grants view-only access to all stealth # outputs; the spend private key grants full spend access. # Store these with the same care as the Dilithium node key. BTCV28_STEALTH_SCAN_PRIV=your_scan_private_key_hex BTCV28_STEALTH_SPEND_PUB=your_spend_public_key_hex # Interval in seconds between background stealth scans (default: 60) BTCV28_STEALTH_SCAN_INTERVAL=60 

Dockerfile

FROM python:3.11-slim RUN apt-get update && apt-get install -y \ libleveldb-dev \ build-essential \ cmake \ git \ libssl-dev \ curl \ && curl -fsSL https://deb.nodesource.com/setup_20.x | bash - \ && apt-get install -y nodejs \ && npm install -g snarkjs circom \ && rm -rf /var/lib/apt/lists/* WORKDIR /app COPY requirements.txt . RUN pip install --no-cache-dir -r requirements.txt COPY btcv28/ btcv28/ COPY tla/ tla/ COPY scripts/ scripts/ COPY pyproject.toml . RUN pip install -e . COPY btcv28/crypto/circuits/ /var/lib/btcv28/circuits/ 2>/dev/null || true RUN chmod +x scripts/setup_circuits.sh EXPOSE 8080 8767 CMD ["python", "-m", "btcv28.node"] 

docker-compose.yml

# Improvement #12: version field removed (deprecated in Docker Compose v2) services: btcv28-node: build: . ports: - "8080:8080" - "8767:8767" environment: - BTCV28_REDIS_HOST=redis - BTCV28_DB_PATH=/data/db - BTCV28_SNAPSHOT_DIR=/data/snapshots - BTCV28_KEYSTORE_PATH=/data/keys - BTCV28_CIRCUITS_DIR=/data/circuits env_file: - .env volumes: - btcv28_data:/data depends_on: - redis redis: image: redis:7-alpine ports: - "6379:6379" volumes: - redis_data:/data prometheus: image: prom/prometheus:latest volumes: - ./prometheus.yml:/etc/prometheus/prometheus.yml ports: - "9090:9090" volumes: btcv28_data: redis_data: 

Summary of All Changes from BTCv28

#ImprovementFiles ChangedDescription1ZKP fails closedcrypto/zkp.py, core/exceptions.py, core/transaction.py, tests/unit/test_zkp.pyZKProofUnavailableError raised instead of silently returning True2SupplyConservationtla/ScriptExecution.tlaFormally specifies total coins = free balances + HTLC-locked3Dispute seq_numconsensus/payment_channel.py, tests/unit/test_payment_channel.py> not >=; equal seq_num raises ChannelError4JWT secret validationapi/auth.py, node.py, .env.exampleassert_jwt_secret_configured() at startup5Mining non-blockingconsensus/pow.pymine_block_async() uses run_in_executor()6Redis cache invalidationstorage/address_index.py_invalidate_cache() on both add/remove paths7Sync lock during restorecore/chain.py, network/sync.py, api/routes.pyis_syncing flag; 503 while catching up8HeaderStore validates PoWstorage/header_store.pyvalidate_pow=True; ValidationError on invalid headers9Channel property teststests/property/test_channel_properties.pyHypothesis tests for capacity, seq monotonicity, dispute10Warning on double-removestorage/address_index.pylogger.warning(...) on missing UTXO11ZK circuit setup scriptedscripts/setup_circuits.sh, crypto/zkp.py, node.pyassert_keys_ready() at startup12docker-compose moderniseddocker-compose.ymlversion: field removed13Package renamed btcv28All filesConsistent naming throughout14Stealth addressescrypto/stealth.py (NEW), core/transaction.py, core/chain.py, core/exceptions.py, storage/address_index.py, consensus/validator.py, wallet/stealth_wallet.py (NEW), api/routes.py, node.py, requirements.txt, .env.example, tests/unit/test_stealth.py (NEW), tests/property/test_stealth_properties.py (NEW)Full DKSAP stealth address protocol: recipient unlinkability, view-tag scanning, PQ-ready hybrid design